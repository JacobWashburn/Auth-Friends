import React from 'react';
import styled from 'styled-components';

const NavWrapper = styled.div`
  display: flex;
  flex-direction: column;
`;
const Dashboard = props => {
    return (
        <NavWrapper>Dashboard</NavWrapper>
    );
};

export default Dashboard;